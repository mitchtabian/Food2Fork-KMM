object AndroidX {
    private const val appCompatVersion = "1.3.0-rc01"
    const val appCompat = "androidx.appcompat:appcompat:$appCompatVersion"

    private const val lifecycleProcessVersion = "2.3.1"
    const val lifecycleProcess = "androidx.lifecycle:lifecycle-process:$lifecycleProcessVersion"
}