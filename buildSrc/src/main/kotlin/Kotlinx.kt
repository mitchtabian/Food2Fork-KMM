object Kotlinx {

    // Might need to consolidate this
    private const val kotlinxCoroutinesVersion = "1.3.2"
    const val coroutinesAndroid = "org.jetbrains.kotlinx:kotlinx-coroutines-android:${kotlinxCoroutinesVersion}"
    const val coroutinesCore = "org.jetbrains.kotlinx:kotlinx-coroutines-core:${kotlinxCoroutinesVersion}"

    private const val kotlinxSerializationVersion = "1.1.0"
    const val serializationJson = "org.jetbrains.kotlinx:kotlinx-serialization-json:${kotlinxSerializationVersion}"
    const val serializationCore = "org.jetbrains.kotlinx:kotlinx-serialization-core:${kotlinxSerializationVersion}"

    private const val kotlinxDatetimeVersion = "0.1.1"
    const val datetime = "org.jetbrains.kotlinx:kotlinx-datetime:${kotlinxDatetimeVersion}"
}